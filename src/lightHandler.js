export class LightHandler
{
    constructor(lights, num_lights, max_num_lights)
    {
        this.lights = lights
        this.num_lights = num_lights
        this.max_num_lights = max_num_lights
    }
}